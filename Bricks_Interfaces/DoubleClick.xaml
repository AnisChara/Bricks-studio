<Window x:Class="Bricks_Interfaces.DoubleClick"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bricks_Interfaces"
        mc:Ignorable="d"
        Title="DoubleClick" Height="300" Width="150"
        WindowStyle="None">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Champ Width -->
        <TextBlock Text="Width:" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Column="1" Margin="5" Text="{Binding Entity.width, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Champ Height -->
        <TextBlock Text="Height:" Grid.Row="1" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="5" Text="{Binding Entity.height, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Poids:" Grid.Row="2" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Column="1" Grid.Row="2" Margin="5" Text="{Binding Entity.weight, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Dégats:" Grid.Row="3" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Column="1" Grid.Row="3" Margin="5" Text="{Binding Entity.damage, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Image:" Grid.Row="4" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Column="1" Grid.Row="4" Margin="5" Text="{Binding Entity.image, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Boutons -->
        <StackPanel Grid.Row="5" Grid.ColumnSpan="2" Orientation="Vertical" HorizontalAlignment="Center" Margin="5">
            <Button Content="Sauvegarder" Width="100" Margin="5" Click="SaveButton_Click"/>
            <Button Content="Supprimer" Width="100" Margin="5" Click="DeleteButton_Click"/>
            <Button Content="Annuler" Width="100" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
