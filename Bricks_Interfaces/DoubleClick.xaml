<Window x:Class="Bricks_Interfaces.DoubleClick"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bricks_Interfaces"
        mc:Ignorable="d"
        Title="DoubleClick" Height="300" Width="150"
        WindowStyle="None">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <!-- Champ Width -->
        <TextBlock Text="Largeur:" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Column="1" Margin="5" Text="{Binding Entity.width, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Champ Height -->
        <TextBlock Text="Hauteur:" Grid.Row="1" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Column="1" Grid.Row="1" Margin="5" Text="{Binding Entity.height, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Poids:" Grid.Row="2" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Column="1" Grid.Row="2" Margin="5" Text="{Binding Entity.weight, UpdateSourceTrigger=PropertyChanged}"/>

        <TextBlock Text="Dégats:" Grid.Row="3" VerticalAlignment="Center" Margin="0,5" Visibility="{Binding DamageVisibility}" />
        <TextBox Grid.Column="1" Grid.Row="3" Margin="5" Text="{Binding Entity.damage, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding DamageVisibility}"/>
        
        <TextBlock Text="Vitesse:" Grid.Row="4" VerticalAlignment="Center" Margin="0,5" Visibility="{Binding SpeedVisibility}" />
        <TextBox Grid.Column="1" Grid.Row="4" Margin="5" Text="{Binding Entity.speed, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding SpeedVisibility}"/>

        <TextBlock Text="Track :" Grid.Row="5" VerticalAlignment="Center" Margin="0,5" Visibility="{Binding TrackVisibility}" />
        <CheckBox IsChecked="{Binding Entity.Track}" Grid.Column="1" Grid.Row="5" Margin="5"  Visibility="{Binding TrackVisibility}"/>

        <TextBlock Text="Collision :" Grid.Row="6" VerticalAlignment="Center" Margin="0,5" Visibility="{Binding CollisionVisibility}" />
        <CheckBox IsChecked="{Binding Entity.is_collidable}" Grid.Column="1" Grid.Row="6" Margin="5"  Visibility="{Binding CollisionVisibility}"/>

        <TextBlock Text="Vie :" Grid.Row="7" VerticalAlignment="Center" Margin="0,5" Visibility="{Binding HealthVisibility}" />
        <TextBox Grid.Column="1" Grid.Row="7" Margin="5" Text="{Binding Entity.max_health, UpdateSourceTrigger=PropertyChanged}" Visibility="{Binding HealthVisibility}"/>

        <TextBlock Text="Image:" Grid.Row="8" VerticalAlignment="Center" Margin="0,5"/>
        <TextBox Grid.Column="1" Grid.Row="8" Margin="5" Text="{Binding Entity.image, UpdateSourceTrigger=PropertyChanged}"/>

        <!-- Boutons -->
        <StackPanel Grid.Row="10" Grid.ColumnSpan="2" Orientation="Vertical" HorizontalAlignment="Center" Margin="5">
            <Button Content="Sauvegarder" Width="100" Margin="5" Click="SaveButton_Click"/>
            <Button Content="Supprimer" Width="100" Margin="5" Click="DeleteButton_Click" Visibility="{Binding DeleteVisibility}"/>
            <Button Content="Annuler" Width="100" Margin="5" Click="CancelButton_Click"/>
        </StackPanel>
    </Grid>
</Window>
