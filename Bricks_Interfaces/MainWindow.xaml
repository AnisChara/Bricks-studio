<Window x:Class="Bricks_Interfaces.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Bricks_Interfaces"
        xmlns:UserControls="clr-namespace:Bricks_Interfaces.Views"
        mc:Ignorable="d"
        Title="Bricks Studio" Height="450" Width="800"
        Background="#2e91f7">

    <DockPanel>

        <Menu DockPanel.Dock="Top">
            <MenuItem Header="_Fichier">
                <MenuItem Header="_Nouveau"/>
                <MenuItem Header="_Ouvrir"/>
                <Separator/>
                <MenuItem Header="_Quitter"/>
            </MenuItem>
            <MenuItem Header="_Édition">
                <MenuItem Header="_Copier"/>
                <MenuItem Header="_Coller"/>
            </MenuItem>
            <MenuItem Header="_Affichage"/>
            <MenuItem Header="_Démarrer"/>
        </Menu>

        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="25"/>
                <RowDefinition Height="*"/>
            </Grid.RowDefinitions>

            <Grid Grid.Row="0">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <StackPanel Grid.Column="1">
                    <WrapPanel>
                        <Button Content="Lancer le jeu" Margin="2" Width="80" HorizontalAlignment="Left" Command="{Binding CompileCommand}"/>
                    </WrapPanel>
                </StackPanel>
            </Grid>

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="3*"/>
                </Grid.ColumnDefinitions>

                <Border Grid.Column="0" Margin="8 8 0 8" Background="LightGray">
                    <UserControls:Onglets x:Name="onglets"/>
                </Border>

                <Grid Grid.Column="2" Margin="0 8 8 8">
                    <Grid.RowDefinitions>
                        <RowDefinition Height="3*"/>
                        <RowDefinition Height="5"/>
                        <RowDefinition Height="2*"/>
                    </Grid.RowDefinitions>

                    <Border Grid.Row="0" Background="LightBlue" BorderThickness="4" BorderBrush="#bc0a0f">
                        <UserControls:Rendu_statique x:Name="visuel"/>
                    </Border>

                    <Border Grid.Row="2" BorderThickness="4" BorderBrush="#bc0a0f">
                        <UserControls:Assets x:Name="assets"/>
                    </Border>
                </Grid>
            </Grid>
        </Grid>
    </DockPanel>
</Window>
