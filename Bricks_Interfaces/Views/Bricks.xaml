<UserControl x:Class="Bricks_Interfaces.Views.Bricks"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:Bricks_Interfaces.Views"
             xmlns:UserControls="clr-namespace:Bricks_Interfaces.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <UserControl.Resources>
        <Style TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </UserControl.Resources>


    <Grid Width="auto" Height="auto"  Background="White">
        <!-- Grille de fond -->

        <Grid >
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="{Binding ActionMenuSize}"/>
                <ColumnDefinition/>
            </Grid.ColumnDefinitions>

            <Border Grid.Column="0"
                Background="#2e91f7"
                BorderBrush="#FFCCCCCC"
                BorderThickness="1"
                CornerRadius="4">
                <StackPanel>
                    <UserControls:Actions x:Name="Actions"/>
                </StackPanel>
            </Border>

            <StackPanel Background="WhiteSmoke" Grid.Column="1" >
                <Grid >
                    <Grid.RowDefinitions>
                        <RowDefinition Height="{Binding EventMenuSize}"></RowDefinition>
                        <RowDefinition></RowDefinition>
                    </Grid.RowDefinitions>
                        <StackPanel Grid.Row="0" Background="#ff5f42" Orientation="Horizontal">
                        <UserControls:Events x:Name="Events"/>
                        <!-- <TextBlock  Text="{Binding Debug, UpdateSourceTrigger=PropertyChanged}" Foreground="White"></TextBlock> -->
                    </StackPanel>
                    <StackPanel Grid.Row="1" PreviewMouseMove="Button_PreviewMouseMove" Name="Board">
                        <ItemsControl ItemsSource="{Binding Events}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Width="{Binding width}" Height="{Binding height}" Margin="{Binding margin}" PreviewMouseLeftButtonDown="Button_MouseLeftButtonDown" 
PreviewMouseLeftButtonUp="Button_PreviewMouseLeftButtonUp" PreviewMouseRightButtonDown="Button_MouseRightButtonDown" CommandParameter="{Binding .}" PreviewMouseRightButtonUp="Button_PreviewMouseRightButtonUp">
                                        <Button.Content>
                                            <Grid>
                                                <Image Source="{Binding Image}" Stretch="Fill" />


                                                <Rectangle Fill="#5000FF00" Visibility="Collapsed"/>

                                                <TextBlock Text="{Binding Text}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" />
                                                <Grid.Background>
                                                    <SolidColorBrush Color="White"></SolidColorBrush>
                                                </Grid.Background>
                                            </Grid>
                                        </Button.Content>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                        <ItemsControl ItemsSource="{Binding Actions}">
                            <ItemsControl.ItemsPanel>
                                <ItemsPanelTemplate>
                                    <Canvas />
                                </ItemsPanelTemplate>
                            </ItemsControl.ItemsPanel>
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Button Width="{Binding width}" Height="{Binding height}" Margin="{Binding margin}" PreviewMouseLeftButtonDown="Button_MouseLeftButtonDown" 
PreviewMouseLeftButtonUp="Button_PreviewMouseLeftButtonUp" PreviewMouseRightButtonDown="Button_MouseRightButtonDown" CommandParameter="{Binding .}" PreviewMouseRightButtonUp="Button_PreviewMouseRightButtonUp">
                                        <Button.Content>
                                            <Grid>
                                                <Image Source="{Binding Image}" Stretch="Fill" />


                                                <Rectangle Fill="#5000FF00" Visibility="Hidden"/>

                                                <TextBlock Text="{Binding Text}" VerticalAlignment="Center" HorizontalAlignment="Center" Foreground="Black" />
                                                <Grid.Background>
                                                    <SolidColorBrush Color="White"></SolidColorBrush>
                                                </Grid.Background>
                                            </Grid>
                                        </Button.Content>
                                    </Button>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </StackPanel>

                </Grid>
            </StackPanel>
        </Grid>
    </Grid>
</UserControl>
