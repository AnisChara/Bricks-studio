<UserControl x:Class="Bricks_Interfaces.Views.Rendu_statique"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Bricks_Interfaces.Views"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800">

    <Grid PreviewMouseMove="Button_PreviewMouseMove" Width="800" Height="450">
        <Grid.Background>
            <ImageBrush ImageSource="{Binding Background}" />
        </Grid.Background>
        <!-- Grille de fond -->
            <ItemsControl ItemsSource="{Binding Entities}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <Canvas />
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate>
                        <Button PreviewMouseLeftButtonDown="Button_MouseLeftButtonDown" CommandParameter="{Binding .}" 
                                PreviewMouseLeftButtonUp="Button_PreviewMouseLeftButtonUp"
                                MouseRightButtonDown="Button_MouseRightButtonDown"
                                Width="{Binding width}" Height="{Binding height}" Background="{Binding color}" Margin="{Binding margin}">
                        </Button>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
    </Grid>
</UserControl>
